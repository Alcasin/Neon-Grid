# M7 Phase A — Power Station 01–03

This vertical slice uses the existing generic campaign and gameplay runtime. The manually authored
`PS_01`, `PS_02`, and `PS_03` LevelDefinition assets are production-candidate content and are not
generated by editor fixtures.

## Future tutorial hooks (planning only)

- PS_01: “Tap a wire to rotate it.”
- PS_02: “Corner wires redirect the current.”
- PS_03: no explicit tutorial; the player applies the previous lessons, including rotating the same
  tile multiple times.

No tutorial objects, triggers, persistence flags, or UI are implemented in Phase A.
